import { Spectral } from "next/font/google";

export const TypeSecond = Spectral({
  weight: ["700"],
  subsets: ["latin"],
  style: ["normal"],
  variable: "--type-second",
  display: "swap",
});
