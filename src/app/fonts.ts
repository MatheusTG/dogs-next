import { Spectral } from "next/font/google";

export const fontTitle = Spectral({
  weight: ["700"],
  style: ["normal"],
  subsets: ["latin"],
  display: "swap",
  variable: '--font-title'
});
